{
  "name": "eT2F",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "dev": "polymer serve --port 3000 & npm run watch",
    "test": "polymer test",
    "watch": "browser-sync start --proxy localhost:3000 --files 'src/*.html, src/*.js, images/*' --no-ghost-mode "
  },

  "engines": {
    "node": ">=4.0"
  },
  "private": true,
  "devDependencies": {
    "browser-sync": "^2.17.0",
    "del": "^2.2.1",
    "gulp": "gulpjs/gulp#4.0",
    "gulp-if": "^2.0.1",
    "gulp-imagemin": "^3.0.1",
    "merge-stream": "^1.0.0",
    "plylog": "^0.4.0",
    "polymer-build": "^0.4.0"
  }
}
