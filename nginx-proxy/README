This nginx.conf file is able to proxy the frontend docker image and the backend to emulate the staging server.

Steps:
- install nginx with brew:
-- brew tap homebrew/nginx
-- brew install nginx-full
- run nginx -c ~/ABSOLUTE/PATH/TO/ET2F/PROJECT/nginx-proxy/nginx.conf (absolute path to this file)
- install docker
-  enter the project directory
- docker build -t t2f-frontend .
- docker run -d -p 8080:8080 t2f-frontend
- run the backend with: python EquiTrack/manage.py runserver 0.0.0.0:8000
- browse localhost/t2f/
